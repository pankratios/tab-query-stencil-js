# TabQuery chrome extension with stencil js [WIP]

* use ```<script src="build/app.js"``` or the hash of the inline script needs to be added to the manifest (if hash changes the extension needs to be reloaded)
